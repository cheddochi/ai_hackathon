[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'TESSDATA_PREFIX=/app/tessdata uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
